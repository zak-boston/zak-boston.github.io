---
title: "Project Euler #1 – Multiples of 3 or 5"
date: 2025-11-19
problem_num: 1                     # ← fixes the typo (problem-num → problem_num)
categories: euler
tags: easy python one-pass modulo
layout: post
---

<div style="text-align:center; margin-bottom:3rem; padding:2rem; background:rgba(88,166,255,0.1); border-radius:12px; border-left:6px solid #58a6ff;">
  <h1 style="margin:0; font-size:2.8rem; color:#58a6ff;">
    Problem {{ page.problem_num }}
  </h1>
  <p style="font-size:1.4rem; margin:1rem 0 0; opacity:0.9;">
    Multiples of 3 or 5
  </p>

  <!-- Auto-generated tag badges (no more manual <span> needed!) -->
  <div style="margin-top:1.5rem;">
    {% for tag in page.tags %}
      {% case tag %}
        {% when 'easy' %}<span class="tag tag-easy">Easy</span>
        {% when 'medium' %}<span class="tag tag-medium">Medium</span>
        {% when 'hard' %}<span class="tag tag-hard">Hard</span>
        {% when 'python' %}<span class="tag tag-python">Python</span>
        {% when 'math' %}<span class="tag tag-dp">Math</span>
        {% when 'number-theory' %}<span class="tag tag-one-pass">Number Theory</span>
        {% else %}<span class="tag" style="background:#6f42c1;">{{ tag | capitalize }}</span>
      {% endcase %}
    {% endfor %}
  </div>
</div>

### Problem Statement

If we list all the natural numbers below 10 that are multiples of 3 or 5, we get 3, 5, 6 and 9. The sum of these multiples is 23.

**Find the sum of all the multiples of 3 or 5 below 1000.**

[Official Problem →](https://projecteuler.net/problem=1)

### Solution – One-liner (Python)

```python
sum(x for x in range(1, 1000) if x % 3 == 0 or x % 5 == 0)
# → 233168